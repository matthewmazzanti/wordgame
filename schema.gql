type Game {
  id: ID!
  letters: String!
  guessed: [String!]!
}

type Query {
  game(id: ID!): Game!
}

type Mutation {
  createGame: Game!
  addGuess(guess: String!): Game!
}

type Subscription {
  watchGame: Game!
}
